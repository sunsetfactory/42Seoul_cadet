#include "mutantstack.hpp"

// But why would we accept this?
// Especially if we can take the liberty of butchering the original stack to
// create missing features.
// 하지만 왜 우리가 이것을 받아들이겠습니까?
// 특히 우리가 자유롭게 원래 스택을 잘라 누락된 기능을 만들 수 있다면 말입니다.

// Of course, you will write and turn in your own tests to ensure everything
// works as expected.
// 물론 모든 것이 예상대로 작동하는지 확인하기 위해 직접 테스트를 작성하고
// 제출할 것입니다.

// If you run it a first time with your MutantStack,
// and a second time replacing the MutantStack with, for example, a std::list,
// the two outputs should be the same.
// MutantStack로 처음 실행하고, 두 번째로 MutantStack을 예를 들어 std::list로
// 대체하면 두 출력이 동일해야 합니다.

// Of course, when testing another container, update the code below with the
// corresponding member functions (push() can become push_back()).
// 물론 다른 컨테이너를 테스트할 때 아래 코드를 해당 멤버 함수로
// 업데이트하십시오 (push()는 push_back()이 될 수 있습니다).
